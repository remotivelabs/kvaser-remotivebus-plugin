#!/bin/bash
set -e

# Reload systemd daemon
systemctl daemon-reload

# Enable and start the service
systemctl enable kvaser-remotivebus-plugin.service
systemctl start kvaser-remotivebus-plugin.service

exit 0
